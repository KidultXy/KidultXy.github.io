import { DisplayObject } from '@antv/g';
/**
 * Show tooltip for series item.
 */
export declare function seriesTooltip(root: DisplayObject, { elements: elementsof, sort: sortFunction, filter: filterFunction, scale, coordinate, crosshairs, render, groupName, wait, leading, trailing, startX, startY, body, single, position, enterable, style: _style, ...rest }: Record<string, any>): () => void;
/**
 * Show tooltip for non-series item.
 */
export declare function tooltip(root: DisplayObject, { elements: elementsof, scale, render, groupName, sort: sortFunction, filter: filterFunction, wait, leading, trailing, groupKey, // group elements by specified key
single, position, enterable, }: Record<string, any>): () => void;
export declare function Tooltip(options: any): (target: any, viewInstances: any) => () => void;
