import type { DisplayObject } from '../../../shapes';
import type { AxisStyleProps, HideOverlapCfg } from '../types';
type Hide = (item: DisplayObject) => void;
type Show = (item: DisplayObject) => void;
export type Utils = {
    hide: Hide;
    show: Show;
};
export default function equidistance(labels: DisplayObject[], overlapCfg: HideOverlapCfg, attr: Required<AxisStyleProps>, utils: Utils): void;
export {};
